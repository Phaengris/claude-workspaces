# T-1_add-widgets

- task: T-1
- description: Add widgets to the thing
- created: 2026-08-09T12:00:00Z

Generated by `workspace` and rewritten on every new/checkout — keep durable notes in CLAUDE.md instead.

## Values

```
PORT0=5002
PORT1=5003
```

## Projects

### app

- dir: app
- branch: T-1

Run `bin/rails s` on ${PORT0}.

Ask before touching the schema.

Services — not started automatically; start what you need with
`workspace up T-1_add-widgets <daemon>` and follow it with
`workspace logs T-1_add-widgets <daemon>`:

- rails — app server — UI at http://localhost:5002
- worker
